spring:
  application:
    name: fileuploader-attacker

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

minio:
  endpoint: http://minio:9000
  access-key: admin
  secret-key: password123
  bucket: images


server:
  port: 33000